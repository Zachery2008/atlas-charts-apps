fullnameOverride: ""
nameOverride: ""

enabled: true

image:
  # Leave registry/tag empty so gitops global.image.registry and
  # catalog-api.image.tag overlays win. name is the image repository.
  registry: ""
  name: atlas-catalog-api
  tag: ""
  pullPolicy: ""

service:
  type: ""
  port: 80
  targetPort: 8080
  annotations: {}

health:
  readinessPath: /actuator/health/readiness
  livenessPath: /actuator/health/liveness

resources: {}

configmap:
  enabled: true

# Pod env (ConfigMap). Merged with global.configurations; service keys win.
configurations: {}
